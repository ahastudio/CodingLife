# Spring Boot CLI with Groovy

## Install

sdkman http://sdkman.io/

```
$ curl -s get.sdkman.io | bash
$ source "$HOME/.sdkman/bin/sdkman-init.sh"
$ sdk version
```

```
$ sdk install springboot
$ spring --version
```
